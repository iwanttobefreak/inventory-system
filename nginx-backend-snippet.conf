# ========================================
# COPY/PASTE: Agregar al bloque server {} de nginx
# Posición: ANTES de "location / {"
# ========================================

    # Backend API (Puerto 4000 en tu Mac)
    location /api/ {
        proxy_pass http://192.168.1.84:4000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Health check del backend
    location /health {
        proxy_pass http://192.168.1.84:4000/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

# ========================================
# Después de esto, debería estar tu "location / {"
# ========================================
